# Default: browser on same machine as API
VITE_API_BASE_URL=http://localhost:5000

# Real phone on same Wi‑Fi (replace 192.168.x.x with your PC’s IPv4 from ipconfig):
# VITE_API_BASE_URL=http://192.168.x.x:5000
# Then open http://192.168.x.x:8080 on the phone (npm run dev; backend on :5000).
