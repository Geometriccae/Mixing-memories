# Frontend environment variables (Vite).
# Vite only exposes variables to browser code if they start with `VITE_`.

# --- AUTOMATIC API DETECTION ---
# The app automatically switches to localhost if running on 'localhost' or '127.0.0.1'.
# Otherwise, it uses the production URL below.

# Production URL
VITE_API_BASE_URL=https://backend.theroyaloven.com

# Local URL (Used automatically when on localhost)
VITE_LOCAL_API_BASE_URL=http://localhost:5000

# --- EmailJS (Get In Touch contact form) ---
# Create a free account at https://www.emailjs.com/
# Service ID: Email Services → your service
# Template ID: Email Templates → your template (use {{from_name}}, {{reply_to}}, {{message}})
# Public Key: Account → API Keys → Public Key
VITE_EMAILJS_SERVICE_ID=service_q2fg5mc
VITE_EMAILJS_TEMPLATE_ID=template_hlecrrq
VITE_EMAILJS_PUBLIC_KEY=x9JXZ1DW5GkVgBHWG
